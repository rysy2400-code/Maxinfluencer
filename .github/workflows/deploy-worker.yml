name: Deploy Worker (Windows VM)

on:
  push:
    branches: [main]
    paths:
      - "scripts/process-*.js"
      - "scripts/poll-*.js"
      - "scripts/run-*.js"
      - "scripts/crawler-health-checker.js"
      - "lib/agents/**"
      - "lib/execution/**"
      - "lib/heartbeat/**"
      - "lib/db/**"
      # worker 的邮件事件链路依赖 lib/influencer/**（如 country-reply-sync、business-profile），
      # 缺了这条会导致只改 lib/influencer 时 worker 拿不到新代码
      - "lib/influencer/**"
      - "package.json"
      - "package-lock.json"
      - "deploy-worker.ps1"
      - "register-windows-scheduled-tasks.ps1"
      - ".github/workflows/deploy-worker.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy worker over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 152.32.216.107
          username: administrator
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 22
          script_stop: true
          # 机器 A：git pull + npm ci；任务 7 在爬虫机 deploy-crawler。首次注册 1-6 计划任务时设 REGISTER_MAXIN_SCHEDULED_TASKS=1。
          script: |
            powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\maxinfluencer\deploy-worker.ps1
